# 棚卸し完了

作成日: 2026-05-07

## 2026-05-07 実施済み

手堅い範囲として、C011の過剰な仕様範囲を現ソースに合わせ、古いドキュメント記述を更新した。

## 2026-05-07 追加実施

- C011再拡張の段階1として `getTitleRenderModel` を追加。
- C011再拡張の段階2として `getHeroineSelectRenderModel` と `getTurnResultRenderModel` を追加。
- C011 spec/testspec にTITLE Render Modelを復帰。
- C011 spec/testspec にHEROINE_SELECT / TURN_RESULT Render Modelを復帰。
- C011単体テストを7件に拡張。
- C011 Render Modelをブラウザ具体層のTITLE / HEROINE_SELECT / TURN_RESULTで段階利用。
- C012を正式contractではなく統合実装メモ / non_contract 扱いへ整理。
- C012のブラウザ戻るボタン制御要件を仕様対象外へ整理。
- ヒロインID別プリロード呼び出しを明示化。
- `browser/app.js` から typewriter / turnTransition / input handling を抽出。
- CSS TODO2 の `continue stub` 表現を現在実装に合わせて言い換え。
- 生成物と巨大データの扱いを `docs/source_structure.md` に記録。
- ブラウザ手動確認の委任項目を `docs/human_verification.md` に記録。

## 変更内容

- `src/core/renderModel.cjs`
  - VN/RHYTHMの現行挙動を維持したまま、speaker / standing / rhythm choices 生成を小さく分割。
  - TITLE / HEROINE_SELECT / TURN_RESULT の純粋Render Modelを追加。
- `tests/core/C011_RENDER_MODEL.test.cjs`
  - VNナレーションケースを追加。
  - Rhythm choices / stats の検証を追加。
  - TITLE / HEROINE_SELECT / TURN_RESULT の検証を追加。
- `specs/modules/C011_RENDER_MODEL.spec.md`
  - TITLE / HEROINE_SELECT / VN / RHYTHM / TURN_RESULT の純粋Render Model契約として整理。
- `specs/modules/C011_RENDER_MODEL.testspec.md`
  - 余計なMarkdownフェンスを除去。
  - テスト対象をTITLE / HEROINE_SELECT / VN / RHYTHM / TURN_RESULTに整理。
- `specs/modules/C012_BROWSER_SYSTEM_IMPLEMENTATION.spec.md`
  - 正式contractではなく、ブラウザ統合実装メモとして整理。
- `specs/modules/C012_BROWSER_SYSTEM_IMPLEMENTATION.testspec.md`
  - 正式testspecではなく、ブラウザ統合確認メモとして整理。
- `.kawazu/construct/project_blueprint.json`
  - C012をcontracts / implementation_orderから外し、non_contract_files側へ整理。
- `specs/integration/I001_BROWSER_PLAYTHROUGH.testspec.md`
  - レジューム未実装記述を、現在ランのオートセーブ復元仕様へ更新。
- `docs/autosave_design_memo.md`
  - 実装済み保存範囲と後続候補に整理。
- `docs/loading_plan.md`
  - 現行BGM/SFX playback policyを追記。
  - ヒロインID別プリロード呼び出しを実装済みとして追記。
- `browser/controllers/typewriterController.js`
  - テキスト表示タイマーとfinish/clear処理を抽出。
- `browser/controllers/turnTransitionController.js`
  - ターン遷移演出タイマーとskip処理を抽出。
- `browser/controllers/inputController.js`
  - document level のselect/drag/click bindingを抽出。
- `browser/screens/titleScreen.js`
  - `getTitleRenderModel` から続きから可否とタイトル文言を参照。
- `browser/screens/heroineSelectScreen.js`
  - `getHeroineSelectRenderModel` からヒロイン候補表示値を参照。
- `browser/screens/turnResultScreen.js`
  - `getTurnResultRenderModel` からターンとスコア差分を参照。

## 2026-05-07 横断リファクタリング（後続担当分）

- TITLE画面のロゴ・時計の比率崩れを修正（`vw`を論理`px`へ変更）。
- 全画面表示時のアスペクト比率維持を修正（fullscreen対象を`document.documentElement`へ変更）。
- クイズBGMの選択ロジック確認と、デバッグジャンプ時の`turn`引数対応。
- タイトルのデバッグボタンを削除し、`tools/debug-links.html`へ集約。
- 画像集（ギャラリー）の初期実装（画像自動走査 manifest 生成含む）。
- イベント集（回想）の初期実装（`src/data/eventMaster.cjs` 抽出）。
- フリープレイの初期実装（BGM・問題数選択対応）。

## 変更内容（追加）

- `browser/styles/10_title.css`
  - ロゴと時計のサイズ単位を論理 720x1280 基準の `px` に固定。
- `browser/utils/debugJump.js`
  - `jump=quiz` 時に `turn` パラメータを `GameSession` に反映するよう修正。
- `tools/sync-gallery.cjs` (新規)
  - `background` と `still` フォルダを走査してギャラリー用 manifest を生成するツール。
- `browser/data/galleryManifest.js` (新規)
  - `sync-gallery.cjs` によって自動生成された画像リスト。
- `src/data/eventMaster.cjs` (新規)
  - イベント集の管理用マスターデータ。
- `browser/screens/titlePanelScreen.js`
  - 画像集、イベント集、フリープレイ設定画面の実装。
- `browser/app.js`
  - `startFreePlay` メソッドの追加。
  - `toggleFullscreen` の対象を `document.documentElement` に変更。
- `browser/controllers/inputController.js`
  - フリープレイ開始アクションのバインド。

## 完了TODO

- [x] C011 の方針を決める。短期はVN/QUIZ補助モデルへ縮小する。
- [x] C011 の spec / testspec / source / test を方針に合わせて同期する。
- [x] C011 testspec の余計な Markdown フェンスを修正する。
- [x] ブラウザ具体層からC011 Render Modelを段階的に利用する。
- [x] I001 testspec のレジューム未実装記述を更新する。
- [x] `docs/autosave_design_memo.md` を実装済み範囲と後続範囲に分ける。
- [x] `docs/loading_plan.md` の BGM エンジン未実装記述を更新する。
- [x] C012 をnon_contractまたは統合実装メモ扱いに揃える。
- [x] C012 の戻るボタン制御要件を仕様から外す。
- [x] ヒロイン別プリロード呼び出し経路を明確化する。
- [x] `browser/app.js` から typewriter を抽出する。
- [x] `browser/app.js` から turnTransition を抽出する。
- [x] `browser/app.js` から input handling を抽出する。
- [x] CSS TODO2 を現在実装に合わせて言い換える。
- [x] `public/style.css` は生成物として扱う旨を明記する。
- [x] タイトル画像比率と全画面表示時のHD縦アスペクト崩れを修正する。
- [x] クイズゲームの利用楽曲ルールを確定し、デバッグでターン指定した際も正常に動作するよう修正する。
- [x] デバッグボタンを削除し、必要なデバッグ機能を `tools/debug-links.html` に集約する。
- [x] イベント集、画像集、フリープレイを計画に沿って段階実装する。

## 検証結果

- `node --test tests/core/C011_RENDER_MODEL.test.cjs`: 成功。
- `node --check browser/app.js`: 成功。
- `node --check browser/screens/titlePanelScreen.js`: 成功。
- `node --check browser/utils/debugJump.js`: 成功。
- `node tools/sync-gallery.cjs`: 成功（24 items）。
- `npm run check:browser`: 成功。

## 未実施として残したもの

- `saveData.js` / `playerProgress.js` / `itemCollection.js` の共通storage helperを検討する。
- タイトル、ヒロイン選択、VN のUI調整TODOを個別チケット化する。
- `.kawazu/construct/reviews` の古いレビューを再生成するか、履歴資料扱いを明記する。
- イベント再生機能（現在はリスト表示のみ）の具体実装。
- 画像拡大表示機能（現在はタイル表示のみ）。
